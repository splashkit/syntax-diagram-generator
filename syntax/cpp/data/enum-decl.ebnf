enum decl. = "enum", [ enum name ], "{", { enum constant }+(","), "}", ";";

enum constant = enumeration constant name, [ "=", constant expression ], ";";

enum name = identifier;

enum const name = identifier;
